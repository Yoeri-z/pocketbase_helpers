name: pocketbase_helpers_cli
description: A cli for pocketbase_helpers, can automatically generate models from a pocketbase schema json.
version: 0.1.0
repository: https://github.com/Yoeri-z/pocketbase_helpers
topics:
  - pocketbase
  - models

executables:
  pb_generate:

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.7.0
  dart_style: ^3.1.7
  path: ^1.9.0
  pocketbase: ^0.23.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
